list:iterate() {
   arg:check 2 "$@" || return;
   typeset list="$1"
   typeset func="$2"
   shift 2
 
   typeset -a items
   typeset item

   set -A items -- $list

   for item in "${items[@]}"; do
      $func "$item" "$@"
   done
}
