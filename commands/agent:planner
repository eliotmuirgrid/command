agent:planner() {
   if [[ -z "$1" ]];  then
      echo "Usage: agent:planner <task>"
      return 1
   fi

   local out="$(agent:file chatgpt $HOME/command/lua/planner.md $1)"


   echo $out |glow
}
