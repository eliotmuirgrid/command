git:local:clone() {
   local repo="$1"
   local dest="$2"

   if [ -z "$repo" ] || [ -z "$dest" ]; then
      echo "Usage: git:local:clone <repo-name> <directory>"
      return 1
   fi

   git clone "git@localhost:/home/git/repos/${repo}.git" "$dest"
}
