arg:check() {
    local expected=$1
    shift

    local actual=$#
    if [[ $actual -ne $expected ]]; then
       print -u2 "🚨 [Error] $(trace:caller) requires exactly ${expected} argument(s), but received ${actual}."
       return 1
    fi
    return 0
}
