git:publish() {
   local msg="${arg:default $1 Update}"
   echo $msg;
   local root="$HOME/$(pwd:top)"
   echo "$root"
   dir:push "$root"

   git $root add . || return
   git $root commit -m "$msg" || return

   git pull --no-edit
   git:push:all
   dir:pop
}
